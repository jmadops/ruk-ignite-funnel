{
  "name": "ohio-ignite-funnel",
  "version": "0.1.0",
  "private": true,
  "description": "Ohio Ignite (May 16) — landing page + Stripe embedded checkout funnel.",
  "scripts": {
    "start": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "stripe": "^17.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
